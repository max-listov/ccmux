---
title: Мёртвый держатель реестра переживает любого ожидающего
description: Ожидающий сдаётся через 10 секунд, а сборка мусора после мёртвого держателя начинается только через 30 — то есть ветка уборки для него недостижима, и каждый вызов упирается в таймаут, пока кто-нибудь не придёт позже.
type: task
status: done
tags: [registry, locking, reliability]
created: 2026-08-25
updated: 2026-08-25
completed: 2026-08-25 23:10 +07:00
---

# Мёртвый держатель реестра переживает любого ожидающего

## Зачем

`withSessionRegistryLock` сериализует каждую транзакцию над реестром сессий между процессами.
У него два порога, и они не согласованы между собой:

| порог | значение | что делает |
|---|---|---|
| `TIMEOUT_MS` | 10 с | ожидающий сдаётся и бросает `session registry lock timed out` |
| `STALE_MS` | 30 с | раньше этого возраста замок не считается протухшим, и `reapDeadLock` выходит сразу |

Ожидающий живёт 10 секунд, а право убрать за мёртвым держателем появляется на 30-й. Значит
**ветка уборки для него недостижима**: процесс, начавший ждать до того, как замок протух, всегда
отвалится по таймауту и ни разу не дойдёт до реапа.

Практическое следствие: держатель, умерший, не сняв замок, блокирует реестр для всех, и первые
двадцать секунд каждый вызов не просто ждёт, а **падает**. Разблокирует ситуацию не механизм, а
случайность — тот, кто попробует позже 30-й секунды.

Обнаружено при чтении кода после падения гейта в CI с этим самым сообщением. ⚠️ Причинная связь
НЕ доказана: тот же коммит на `main` прошёл зелёным, то есть падение было средовым, и приписывать
его этому месту без пробника было бы ровно той ошибкой, которую этот проект запрещает. Дефект
самостоятелен и виден из кода независимо от того прогона.

## Результат

- Ожидающий, столкнувшийся с мёртвым держателем, дожидается права на уборку, а не отваливается
  раньше, чем оно наступает.
- Живой держатель по-прежнему не может быть убран досрочно — соотношение порогов защищает именно
  его, и это свойство обязано сохраниться.
- Долгий, но честный ожидающий отличим от того, кто упёрся в труп: сообщение об отказе говорит,
  что именно произошло.

## Открытые вопросы

- Согласовать пороги или дать ожидающему право реапнуть раньше, доказав смерть владельца по pid.
  Второе точнее: `processAlive` уже проверяется, и возраст замка — лишь грубый прокси к тому же
  вопросу.
- Отдельно посмотреть, не бывает ли утечки замка при удачном `mkdir` и неудачной записи файла
  владельца: тогда создатель не владеет ничем, но каталог остаётся, и это тот же тупик без всякой
  смерти процесса.

## Что сделано

Сначала доказал дефект пробником, а не рассуждением: замок, брошенный секунду назад мёртвым
процессом, **упал по таймауту через 10013 мс**. После правки тот же пробник — **25 мс**.

Заодно выяснилось, почему дефект дожил до сегодня: единственный существовавший тест состаривал
mtime замка на 60 секунд, то есть обходил ровно то условие, которое ломалось.

### Механизм

- [x] `lockVerdict` в `src/config/registryLock.ts` — чистая функция «факты о замке → что с ним
      делать». Решает **pid**: мёртвый владелец реапается в тот же миг, в любом возрасте.
- [x] Возраст теперь отвечает ровно на один вопрос — является ли каталог **без читаемого владельца**
      захватом в полёте (между `mkdir` и записью файла владельца два сисколла) или его обломком.
- [x] `UNCLAIMED_GRACE_MS` (5 с) обязан быть меньше `TIMEOUT_MS` (10 с), иначе ветка уборки снова
      станет недостижимой. Инвариант вынесен в тест, а не только в комментарий.
- [x] Нечитаемый файл владельца трактуется как отсутствующий, а не как разрешение забрать замок:
      это не доказательство ни смерти, ни жизни.
- [x] Создатель, не сумевший занять собственный каталог, убирает его **сразу**, а не оставляет
      дожидаться грейса. Это и была вторая половина находки из открытых вопросов.
- [x] Сообщение таймаута называет, что мешало: pid живого держателя либо отсутствие владельца.
      Ровно этого не хватило, чтобы разобрать сегодняшнее падение гейта.

### Тесты

- [x] `test/registry-lock.test.ts` переписан: 8 проверок, ничего не состаривает искусственно там,
      где проверяется мёртвый владелец. 624 теста зелёные, `tsc --noEmit` чист.

### Проверено вживую

- [x] Мёртвый владелец: было 10013 мс и отказ, стало 25 мс и захват.
- [x] Живой владелец: отказ через 10006 мс с текстом `held by pid 87996` — свойство, которое
      защищал старый возрастной порог, сохранено.
